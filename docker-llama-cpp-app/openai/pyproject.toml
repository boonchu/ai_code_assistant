[project]
name = "openai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "openai",
]

[tool.uv.sources]
openai = { workspace = true }
